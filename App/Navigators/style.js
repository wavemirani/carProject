import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    drawer: {
        marginBottom: 50,
        padding: 0
    },
    drawerHeader: {
        height : 130,
        backgroundColor: "#1B3CFD",
    },
    titleStyle: {
        color: 'white',
        fontWeight: "bold",
        fontSize: 20
    }
})